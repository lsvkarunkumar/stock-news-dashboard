name: Update data

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout latest
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run updater
        env:
          TZ: Africa/Lagos
        run: |
          python -m scripts.update_data

      - name: Cleanup runtime junk
        run: |
          find . -type d -name "__pycache__" -prune -exec rm -rf {} +
          find . -type f -name "*.pyc" -delete
          find . -type f -name "*.db-wal" -delete
          find . -type f -name "*.db-shm" -delete

      - name: Commit & push (stash + rebase safe)
        run: |
          set -e
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          # Stash any changes created by updater (e.g., data/stock.db)
          git add -A
          git stash push -m "ci-stash" || true

          # Rebase onto latest main
          git fetch origin main
          git rebase origin/main || (git rebase --abort && git pull --rebase origin main)

          # Re-apply stashed changes
          git stash pop || true

          # Stage only what we want to commit
          git add data/stock.db data/watchlist.csv || true

          git status

          git commit -m "Auto update data" || echo "No changes to commit"

          git push origin HEAD:main
